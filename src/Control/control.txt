第一步：从BottleDic和ControlDic中读取出相同类型的瓶子和喷嘴之间的位置信息，作差求出DeltaPos；
第二步：收到来自外部的SeePic触发信号或CheckAgain=k*DeltaPos信号触发Control函数；
第三步：Control函数
        如果DeltaPos小于等于某个尺寸，如(5px),
              则向TX2发送控制指令，控制该类型的喷嘴喷射若干时间(Delta_t)；
        返回；
    




